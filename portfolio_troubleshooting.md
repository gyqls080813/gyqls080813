# 포트폴리오 트러블슈팅 정리

---

## 1. Tickle — 공정한 티켓팅 플랫폼 (AI 매크로 탐지)

**사용한 기술 스택** : Next.js 16 · React 19 · TypeScript · Zustand · TanStack React Query · TailwindCSS 4 · Framer Motion · Recharts · SSE · Effect Schema · Canvas API

**기여도** : 20%

**역할** : Frontend Leader

---

### 트러블슈팅 ①: 이벤트 수집 — 브라우저 메인 스레드 성능 저하 방지

**[문제]**
AI 모델의 매크로 탐지를 위해 유저의 마우스 궤적, 클릭 타이밍, 키보드 입력 등 초당 수십~수백 개의 행동 데이터(28개 특성) 수집 필요.
단순하게 `mousemove` 이벤트마다 상태 업데이트 및 서버 전송 시, 브라우저 메인 스레드 블로킹으로 인한 심각한 프레임 드롭(12 FPS까지 급락)이 발생하여 정상 유저의 예매 경험(UI 렌더링 및 클릭 반응 지연 최대 2초) 저하.

**[원인]**
- `mousemove`는 초당 수십~수백 회 발생하는 고빈도 이벤트. 매 이벤트마다 `setState`로 상태 업데이트 시 불필요한 재렌더링이 폭주(10초 기준 약 1,886회)하고, `fetch` 호출 시 대량의 네트워크 요청(약 941회)이 누적되어 메인 스레드 블로킹(최대 427ms) 발생.
- 50ms 주기의 단순 쓰로틀링(Throttling)을 적용해도 여전히 300회 이상의 리렌더링과 미세한 화면 끊김(Micro-stuttering)이 잔존.

**[해결 방법]**
1. **버퍼링(Buffering) 및 언마운트 시점 1회 전송**
   - 모든 이벤트를 즉시 전송하지 않고, 컴포넌트 생명주기와 분리된 순수 JS 클래스(`TrialCollector`) 내부 메모리 배열에 데이터를 축적.
   - 해당 예매 단계가 끝나는(Unmount) 시점이나 특정 조건 충족 시에만 묶어서(Chunk) 비동기 1회 전송.

2. **React 상태 트리와의 완전한 격리**
   - 렌더링을 유발하는 `setState` 대신, 메모리 변수(`useRef` 클로저)를 활용하여 **재렌더링 없이** 데이터를 누적.
   - 이벤트 리스너에 `{ passive: true }` 옵션을 적용하여 브라우저 스크롤 및 렌더링 스레드 지연 방지.

**[결과]**
비최적화 방식 대비 **강제 렌더링 횟수를 1,886회에서 0회로**, **네트워크 요청을 941회에서 단 1회로** 극적으로 단축.
메인 스레드 블로킹 타임을 0ms로 만들어 **60 FPS의 부드러운 화면 전환을 방어**하면서도, 대규모 행동 데이터를 누실 없이 수집하는 최적화된 프론트엔드 수집 파이프라인 구축.

---

### 트러블슈팅 ②: CANVAS — 매크로의 DOM 조작(Console Script) 원천 차단 및 보안 렌더링

**[문제]**
기존 티켓팅 사이트들은 좌석을 HTML DOM(`<div>` 또는 `<button>`) 태그로 렌더링.
매크로 제작자가 브라우저 콘솔에서 `document.querySelector('.seat.available').click()` 한 줄로 잔여 좌석을 자동 선택 가능.
프론트엔드 DOM 구조 자체가 좌석의 위치와 상태를 투명하게 노출하는 구조적 취약점 존재.

**[원인]**
- HTML DOM은 개발자 도구(DevTools)를 통해 구조, 클래스명, 식별자(ID), 속성(data-attribute) 등이 모두 노출됨.
- 매크로 스크립트가 DOM 구조를 파싱하여 좌석 상태를 파악하고, 프로그래밍적으로 클릭 이벤트를 발생시킬 수 있는 구조.
- 백엔드 방어와 무관하게 프론트엔드가 구조적으로 정보를 누출하는 근본적 취약점.

**[해결 방법]**
1. **DOM 구조 은닉(Obfuscation)**
   - 수천 개의 좌석 데이터를 DOM 트리에 렌더링하지 않고, 단일 `<canvas>` 태그 위에 Canvas 2D 벡터 드로잉 API(`roundRect`, `arc`, `fillRect`)로만 렌더링
   - DOM Inspector로 좌석의 구조, 식별자, 상태를 파악 불가하도록 처리

2. **좌표 기반 자체 이벤트 매핑**
   - 좌석 클릭을 DOM `onClick`이 아닌, Canvas 전체의 마우스 `(x, y)` 좌표를 캡처하여 사전 계산된 좌석별 바운딩 박스 좌표 비교(AABB 히트 테스트)로 매핑하는 방식으로 구현
   - 매크로 스크립트가 좌석을 클릭하려면 내부 레이아웃 좌표 역산 또는 이미지 인식(Computer Vision) 도입이 필요하도록 공격 난이도 대폭 상향

**[결과]**
DOM 선택자 조작 방식의 매크로 봇을 프론트엔드 렌더링 단계에서 원천 무력화.
**프론트엔드 렌더링 자체가 보안 방어막(Anti-Bot Shield)으로 기능**할 수 있음을 입증.

---

### 트러블슈팅 ③: WEBDRIVER — 브라우저 핑거프린팅 기반 1차 봇 차단

**[문제]**
백엔드와 AI 서버에서 정밀한 매크로 탐지를 수행하지만, 셀레니움(Selenium)·플레이라이트(Playwright) 등 기초적인 자동화 스크립트의 접근까지 전부 AI 서버(Kafka)로 전달하는 것은 불필요한 서버 리소스 소모.

**[원인]**
- 셀레니움/플레이라이트 등 자동화 도구는 브라우저 객체에 고유 흔적(`navigator.webdriver = true`, DOM 주입 변수 등)을 남김.
- 프론트엔드에서 선제적으로 감지하면 AI 연산 없이 기초적 봇 트래픽 즉시 필터링 가능.
- 기존에는 1차 필터링 없이 모든 트래픽이 AI 파이프라인까지 도달하여 불필요한 리소스 소모.

**[해결 방법]**
1. **프론트엔드 1차 방어선 (`BotDetector.tsx`) 구축**
   - 브라우저 레벨에서 즉각적인 봇 탐지 수행

2. **WebDriver 속성 및 주입 변수 감지**
   - `navigator.webdriver` 속성 `true` 여부 확인
   - 봇 프로그램이 DOM에 주입하는 특정 식별자(`documentElement.getAttribute('webdriver')`) 스캐닝
   - 감지 시 대기열(Queue) 진입 차단 및 봇 차단 페이지로 리다이렉트

**[결과]**
딥러닝 탐지 엔진 이전 단계에서 프론트엔드가 기초적 매크로 트래픽을 선제적으로 필터링하여 전체 시스템 부하(AI 서버 비용) 절감.

> **러닝 포인트 (Tickle 종합)** : 프론트엔드 개발자로서 **보안(Anti-Bot), 성능(Event Throttling), 렌더링 아키텍처(Canvas)** 세 가지 축에서 서비스의 핵심 가치(공정한 티켓팅)를 기술적으로 뒷받침하는 역량 확보.

---
---

## 2. Petfolio — AI 기반 반려동물 공동 가계부 서비스

**사용한 기술 스택** : Next.js (React 19) | TypeScript | React Query | Effect-TS | @effect/schema | Tailwind CSS | Framer Motion | Lottie React

**기여도** : 20%

**역할** : Frontend Leader

---

### 트러블슈팅 ①: 애자일 개발에 맞는 안정적인 프론트엔드 방어 아키텍처 구축

**[문제]**
짧은 기간 내에 빠르게 기능을 이터레이션하는 애자일(Agile) 방식으로 개발 진행.
백엔드 및 AI 서버의 API 스키마가 빈번하게 변경되거나, 불확실한 형태의 응답이 내려오는 과도기 존재.

**[원인]**
- 프론트엔드가 외부 데이터를 맹신(Trust)한 채 개발을 진행한 결과, 스키마 변경 또는 예상치 못한 API 에러(401, 500) 발생 시 렌더링 단에서 `TypeError`가 발생하며 앱이 크래시되는 런타임 오류 빈발.
- 각 API 호출부마다 방어적 조건문(`if-else`)을 통한 에러 처리를 추가했으나, 코드 복잡도 증가 및 누락 시 서비스 장애로 직결되는 구조적 한계 존재.

**[해결 방법]**
외부 데이터를 무조건 신뢰하지 않는 **'Zero-Trust(방어적 프로그래밍)'** 관점을 채택하고, 런타임 스키마 검증이 가능한 `@effect/schema`를 도입.

1. **런타임 스키마 방어막 구축**
   - API 응답 도착 즉시, 프론트엔드 최전방에서 사전 정의한 Schema와의 일치 여부를 런타임 검증 수행
   - 스키마 불일치 또는 AI 응답 파싱 실패 시, 성공으로 처리하지 않고 즉시 규격화된 `ParseError`로 분류하여 렌더링 크래시 사전 차단

2. **에러 격리 UI (`EffectErrorBanner`)**
   - 런타임 검증에서 감지된 에러 태그(`_tag`)를 전역 배너 컴포넌트로 전달
   - 빈 화면(White Screen) 대신 "데이터 형식이 변경되었습니다 (재시도)" 등 맞춤형 예외 UI 제공

**[결과]**
백엔드 스키마 변경 또는 서버 다운 상황에서도, 프론트엔드 앱이 자체적으로 에러를 소화하는 런타임 크래시 프리(Crash-free) 환경 달성.

---

### 트러블슈팅 ②: Effect-TS를 통한 타입 안전한 에러 분기 처리 시스템 구축

**[문제]**
가계부 서비스 특성상 네트워크 오류, 토큰 만료(401), 서버 다운(500), 데이터 파싱 오류 등 다양한 에러마다 유저에게 보여줘야 할 UI와 후속 조치(재시도, 로그인 리다이렉트 등)가 상이.
기존에는 `catch` 블록 내부의 `if-else` 분기문과 `error.message` 문자열 비교로 처리했으나, API 증가에 따라 동일 분기 코드가 중복되어 유지보수성 저하.

**[원인]**
TypeScript 환경에서 `catch(e)`로 넘어온 에러 객체는 `unknown` 타입으로 추론됨.
에러 케이스 중 하나라도 분기 처리를 누락해도 **컴파일러가 이를 감지하지 못하는** 구조적 한계 존재.
개발자의 기억력에 의존하는 취약한 구조로, 분기 누락이 런타임 크래시로 직결.

**[해결 방법]**
인적 실수(Human Error)로 인한 에러 분기 누락을 시스템적으로 차단하기 위해, 함수형 프로그래밍 라이브러리 **Effect-TS**를 도입하여 에러 파이프라인 전면 개편.

1. **에러 타입 규격화 (Tagged Error)**
   - 발생 가능한 모든 런타임 에러를 `ApiError`, `NetworkError`, `UnauthorizedError`, `ParseError` 등 명시적 태그가 부여된 타입으로 정의
   - 문자열 비교(`error.message === '...'`) 대신 타입 시스템 기반 에러 식별로 전환

2. **컴파일러 기반 분기 처리 강제화 (Exhaustive Matching)**
   - API 호출 함수가 성공 데이터뿐 아니라 실패 에러 타입도 명시적으로 반환(`Effect<Data, NetworkError | ApiError | ParseError>`)하도록 설계
   - 커스텀 훅(`useEffectQuery`) 내부에서 `Exit.match`를 활용, 명시된 에러 타입 중 **하나의 분기라도 미처리 시 컴파일 에러 발생**하도록 강제

3. **분기 로직과 UI의 분리**
   - 분기 로직을 컴포넌트에서 분리하고, 규격화된 에러 객체를 `EffectErrorBanner` 전역 컴포넌트에 전달하여 에러 태그 기반 맞춤형 UI(메시지, 아이콘, 재시도 버튼) 자동 렌더링

**[결과]**
수십 줄에 달하던 `try-catch` 분기문을 대폭 축소하고, 모든 예외 상황에 누락 없이 대응 가능한 에러 핸들링 시스템 구축.

> **러닝 포인트** : TypeScript 타입 시스템의 한계를 인식하고 함수형 프로그래밍 패러다임(Effect-TS)을 실무에 적용하여, **비즈니스 로직의 분기 복잡도를 구조적으로 억제하는 아키텍처 설계** 경험.

---
---

## 3. Withy — AI 기반 넷플릭스 동기화 뷰어 (브라우저 익스텐션)

**사용한 기술 스택** : Next.js (React 19) | WXT Framework | TypeScript | Zustand | React Query | STOMP/SockJS | Tailwind CSS | Framer Motion

**기여도** : 20%

**역할** : 메인페이지 구현 / 넷플릭스 익스텐션 구현

---

### 트러블슈팅 ①: 넷플릭스 이벤트와 익스텐션의 이벤트 겹침 문제

**[문제]**
넷플릭스 동기화 뷰어(Party)에서 익스텐션 채팅창에 텍스트(스페이스바, 엔터 등)를 입력할 때, 넷플릭스 네이티브 UI가 키보드 이벤트를 강제로 탈취(Hijacking)하는 현상 발생.
유저가 채팅 중 띄어쓰기를 입력하면 영상이 일시정지되는 등, 넷플릭스 단축키와 채팅 입력이 혼재되는 UX 결함 확인.

**[원인]**
브라우저 익스텐션의 Content Script는 보안상 **'격리된 환경(Isolated World)'**에서 동작.
메인 웹 페이지(Main World)의 전역 변수 공간에 바인딩된 넷플릭스 커스텀 플레이어 스크립트를, `preventDefault()`만으로는 제어 불가.

**[해결 방법]**
1. **실행 컨텍스트(Main World) 스크립트 강제 주입**
   - 스크립트 실행 컨텍스트를 `world: 'MAIN'`으로 변경하여, 넷플릭스가 동작하는 브라우저 메인 메모리 공간에 직접 코드 주입
   - 난독화된 넷플릭스 내부 API(`window.netflix...`)에 직접 접근하여 플레이어 제어권 확보

2. **Capture Phase를 활용한 선제적 이벤트 방어**
   - 이벤트 전파 1단계인 캡처링(Capture Phase)에서 브라우저 최상단(`document.addEventListener(..., true)`)의 포커스 이벤트 감시
   - 채팅창(`inputRef`)에 커서가 있을 때만 타겟 이벤트를 감지하여 `e.stopPropagation()`으로 넷플릭스의 탈취를 차단하고 포커스를 즉시 복구하는 로직 구현

3. **MutationObserver를 통한 2차 복구**
   - 넷플릭스 UI가 idle(숨김) 상태로 전환되며 발생하는 포커스 유실을 `MutationObserver`로 감지하여 복구하는 2중 방어 체계 구축

**[결과]**
넷플릭스 직접 조작 의도(단축키)는 정상 동작하도록 유지하면서, 채팅 중 발생하는 포커스 탈취만 선별적으로 방어하여 끊김 없는 시청·소통 환경 제공.

---

### 트러블슈팅 ②: Shadow DOM 기반 네이티브 UI 주입 및 넷플릭스 레이아웃 강제 조정

**[문제]**
넷플릭스 페이지 위에 채팅 사이드바 UI를 주입해야 했으나, 두 가지 핵심 충돌 발생.
첫째, 익스텐션이 주입하는 React 컴포넌트의 CSS(Tailwind 등)가 넷플릭스 기존 스타일과 상호 오염되어 레이아웃이 깨지는 현상.
둘째, 사이드바를 삽입하면 넷플릭스 플레이어 영역이 사이드바 뒤에 가려지거나, 비율이 틀어져 영상이 잘리는 문제.

**[원인]**
- 브라우저 익스텐션의 Content Script가 주입하는 DOM은 호스트 페이지와 동일한 Document 내에 삽입됨. 별도의 격리 없이 CSS를 주입하면 넷플릭스의 기존 클래스명과 충돌하거나, 반대로 넷플릭스의 글로벌 스타일(`body`, `*` 셀렉터 등)이 익스텐션 UI를 오염시키는 양방향 스타일 충돌 발생.
- 넷플릭스 플레이어는 `#appMountPoint`, `.nf-player-container`, `.AkiraPlayer`, `.sizing-wrapper` 등 다층 구조로 `width: 100%`를 강제하고 있어, 단순히 사이드바를 `position: fixed`로 띄우면 플레이어가 여전히 전체 너비를 차지하여 영상이 사이드바 아래로 가려짐.

**[해결 방법]**
1. **Shadow DOM을 활용한 완전한 스타일 격리**
   - WXT 프레임워크의 `createShadowRootUi`와 `cssInjectionMode: 'ui'` 설정을 활용하여 익스텐션 UI를 Shadow DOM 내부에 렌더링
   - Shadow DOM 경계 내에서만 Tailwind CSS가 적용되므로, 넷플릭스 스타일과의 양방향 오염을 원천 차단
   - Shadow DOM 내부에 `ReactDOM.createRoot`로 React 앱을 마운트하여 완전한 React 생태계(React Query, Zustand, WebSocket 등) 유지

2. **넷플릭스 내부 DOM 다층 레이아웃 강제 조정 (`adjustLayout`)**
   - 사이드바(380px) 공간 확보를 위해, 넷플릭스 플레이어의 핵심 DOM 노드 5개(`body`, `#appMountPoint`, `.nf-player-container`, `.AkiraPlayer`, `.sizing-wrapper`)를 일괄 탐색하여 `width: calc(100% - 380px)` 강제 적용
   - 레이아웃 변경 후 `window.dispatchEvent(new Event('resize'))`를 트리거하여 넷플릭스 내부 리사이즈 핸들러가 영상 비율을 재계산하도록 유도
   - 사이드바 닫기 시 주입한 스타일을 원복하여 넷플릭스 원래 레이아웃으로 복원

**[결과]**
넷플릭스의 기존 UI/스타일을 일체 훼손하지 않으면서, 완전히 격리된 React 채팅 사이드바를 네이티브처럼 자연스럽게 주입. 사이드바 토글 시 영상 비율이 정확히 재조정되어 끊김 없는 시청 환경 유지.

---

### 트러블슈팅 ③: Isolated World ↔ Main World 양방향 메시징 아키텍처 설계

**[문제]**
동기화 뷰어의 핵심 기능인 '영상 동기 재생'을 구현하려면, 넷플릭스 플레이어의 재생/일시정지/탐색(Seek) API를 직접 호출해야 함.
그러나 채팅 UI와 비즈니스 로직이 동작하는 Content Script(Isolated World)에서는 넷플릭스의 내부 플레이어 객체(`window.netflix.appContext...`)에 접근 불가.

**[원인]**
- Chrome 익스텐션의 Content Script는 보안상 Isolated World에서 실행되어, 호스트 페이지의 `window` 전역 객체에 접근 불가.
- 넷플릭스 플레이어 API는 `window.netflix.appContext.state.playerApp.getAPI().videoPlayer`라는 난독화된 경로로만 접근 가능하며, 이는 Main World에서만 도달 가능.
- 단순히 DOM의 `<video>` 태그를 직접 조작하는 방식은 넷플릭스의 DRM(Widevine) 보호 및 커스텀 플레이어 로직과 충돌하여 정상 동작하지 않음.

**[해결 방법]**
1. **Main World 전용 스크립트 분리 (`netflix-player.content.ts`)**
   - `world: 'MAIN'` 설정으로 넷플릭스와 동일한 메모리 공간에서 실행되는 별도 스크립트를 등록
   - 해당 스크립트에서 `window.netflix` 객체를 탐색하여 플레이어 인스턴스(`play`, `pause`, `seek`, `getCurrentTime`)를 획득하는 헬퍼 함수(`getNetflixPlayer`) 구현

2. **`postMessage` 기반 Cross-World 통신 프로토콜 설계**
   - Isolated World → Main World: `WIDDY_NETFLIX_CONTROL` 타입 메시지로 재생/정지/탐색 명령 전달
   - Main World → Isolated World: `WIDDY_NETFLIX_IDLE_STATE`, `WIDDY_NETFLIX_FOCUS_CHANGED` 등 넷플릭스 플레이어 상태 변화를 역방향 전달
   - `event.source !== window` 검증으로 외부 메시지 필터링하여 보안 유지

3. **WebSocket 연동을 통한 다자간 동기화**
   - Host 유저의 영상 이벤트(play/pause/seeked)를 `useVideoBridge` 훅에서 감지 → WebSocket으로 서버 전송 → Guest 유저에게 브로드캐스트 → Guest의 Content Script가 `postMessage`로 Main World에 명령 전달하는 전체 파이프라인 구축
   - 5초 간격 Heartbeat로 네트워크 지연 보정, 1초 이내 오차는 무시하는 Smart Seek 로직으로 불필요한 탐색 방지

**[결과]**
Content Script(UI/비즈니스 로직)와 Main World(플레이어 제어)의 역할을 명확히 분리하면서도 실시간 양방향 통신을 구현하여, 다수의 유저가 넷플릭스 영상을 밀리초 단위로 동기화하며 시청할 수 있는 환경 구축.

> **러닝 포인트 (Withy 종합)** : 브라우저 익스텐션 개발에서 **Shadow DOM 기반 스타일 격리**, **DOM 이벤트 생명주기(Capturing/Bubbling) 제어**, **Cross-World 메시징 아키텍처** 세 가지 축을 종합적으로 설계·구현하여, 서드파티 웹 환경(넷플릭스) 위에 네이티브 수준의 UI/기능을 안정적으로 통합하는 역량 확보.

---
---

## 4. SBR — 하·폐수 처리 공정 폭기(Aeration) 최적화 시스템

**사용한 기술 스택** : Python | Raspberry Pi 2 | Arduino | TimeGAN | DQN (Reinforcement Learning) | MLP | Flask

**기여도** : 100% (1인 프로젝트)

**역할** : AI 모델링 및 하드웨어(IoT) 통합 제어 전체 구현

---

### 트러블슈팅 ①: TimeGAN을 활용한 실제 공정 데이터 부족(Data Scarcity) 극복

**[문제]**
강화학습(DQN)과 MLP 회귀 모델 학습에 수만 건 이상의 시계열 데이터 필요.
실제 SBR 공정은 1사이클에 1시간 이상 소요되어, 물리적 시간 제약상 대규모 데이터 직접 수집 불가.

**[해결 방법]**
실제 환경에서 수집한 한정된 센서 데이터(약 300개 유의미 사이클)를 기반으로, 시계열 데이터 생성 특화 딥러닝 모델인 **TimeGAN (Time-series Generative Adversarial Network)** 도입. 실제 암모니아 농도 변화의 트렌드와 물리적 제약을 유지하면서 10,000 Epoch의 GAN 학습을 수행하여 고품질 합성 데이터(Synthetic Data) 대량 증강.

**[결과]**
데이터 부족 환경에서 GAN 기반 데이터 증강을 통해, MLP 사전 학습(Pre-train)과 DQN 시뮬레이션에 필요한 시계열 데이터를 확보하여 AI 파이프라인의 데이터 병목 해소.

---

### 트러블슈팅 ②: 실제 환경을 모사한 SBR 시뮬레이터 구축 및 DQN 보상(Reward) 튜닝

**[문제]**
증강된 데이터를 바탕으로 에이전트(DQN)가 최적의 폭기(Aeration) 시간을 학습해야 하나, 강화학습 환경(Environment)이 현실의 화학적 반응을 정확히 모사하지 못하면 실용성 없는 모델이 됨.

**[해결 방법]**
1. **정밀한 환경 수식화 (`SBR_Environment.py`)**
   - 실제 반응조 스펙(1L 안정화 기준)을 적용하여, 유입수와 잔류수의 희석 비율(1:23) 계산 등 화학적 지식 기반으로 초기 암모니아 농도 및 폭기 시간 대비 배출수 농도 예측 수식 직접 구현

2. **다중 목표 보상 함수(Reward Function) 설계**
   - 단순 폭기 시간 최대화는 전력 낭비를 초래. **"암모니아 배출 농도를 법적 기준치 이하로 유지하면서 폭기 시간(에너지 소모)을 최소화"**하는 두 가지 상충 목표의 균형을 위해 페널티·리워드 가중치 정밀 튜닝

**[결과]**
수동 조작 대비 폭기 시간을 유의미하게 단축하여 에너지를 절감하면서도 안정적인 수질 정화(질산화) 목표를 달성하는 AI 제어 모델 도출.

> **러닝 포인트** : 데이터 부족 환경에서 생성형 AI(TimeGAN)로 데이터를 증강하고, 이를 기반으로 강화학습(DQN)의 환경·보상 함수를 직접 설계·튜닝하여 실제 물리 공정을 최적화한 경험을 통해, **AI 모델링 전 과정(데이터 → 학습 → 적용)을 주도적으로 설계하는 역량** 확보.
